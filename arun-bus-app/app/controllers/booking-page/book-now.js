import Controller from '@ember/controller';
import { inject as controller } from '@ember/controller';
// import BookingPageController from '../controllers/booking-page';

export default class BookingPageBookNowController extends Controller {
  @controller('booking-page') parentController;
}
